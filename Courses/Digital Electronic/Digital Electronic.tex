\documentclass[UTF8]{article}
\usepackage{ctex}
\usepackage{verbatim}
\usepackage{amsmath}
\usepackage{graphicx}
\usepackage{palatino}
\usepackage{tikz}
\usepackage{float}
\usetikzlibrary{shapes.geometric, arrows}

\title{\heiti 数字电子技术基础}
\author{\heiti 庄梓伸}
\date{\heiti \today}

\begin{document}
\maketitle
%目录
\newpage
\begin{center}
\tableofcontents
\end{center}
%设置起始页
\setcounter{page}{0}
\thispagestyle{empty}

%第一章部分
\newpage
\section{数制与码制}

\subsection{数制——进制数}

\subsection{码制}

\subsubsection{BCD码}

    \begin{table}[H] %取消浮动
        \centering
        \begin{tabular}{|l|c|c|c|p{2.5cm}}
        \hline
        8421BCD & 余三码  & BCD & BCD\\
        \hline
        0000    & 0011 & 243 & 34\\
        \hline
                &      &     &\\
        \hline
                &      &     &\\
        \hline
        \end{tabular}
        \caption{BCD码}
        \label{tab:my-table}
    \end{table}

\subsubsection{校验码}

\subsubsection{ASCII码}

\subsection{二进制的数制问题解决}
下面将通过计算机对数的处理方式进行讲述，
我们知道计算机用二进制的方式储存我们的十进制数，在计算机中一个字节(bytes)表示八个bits，每个bit用0或1表示，这样的话一个字节就能表示256个数，
我们将能表示的数的范围称为{\heiti 模}。如果我们只需要表示正数，那我们只需将正的十进制数逐一用二进制表示即可，
但我们出现了负数的概念，那么多出来了的负号要如何表示呢？下面我们先介绍负数的三种表示方式——原码、反码和补码。

\subparagraph{原码的表示方式}
我们用第8位，也即最高位作为符号位，0表示正数，1表示负数。
在这样表示后，我们发现原本表示0~255的数制变成了-127$\sim $127。是的，我们可以发现这里仅有255个数，为什么呢？
因为再当0作为正数时，其二进制表示为0000 0000，而0作为负数时，其二进制表示为1000 0000。所以这是这种表示方式的第一种问题：存在正负零。

然后我们再研究原码的运算，如果我们研究$1+(-1)$这个问题，利用原码我们要怎么操作呢，
首先我们要将-1的符号位符号化为减法运算（事实上，计算机没有减法运算，此处只是为了更好理解，假设出来的），用1(0000 0001)减去1(0000 0001))，再加上符号(1000 0000)，
得到最终答案1000 0000(-0)。
所以，这里又出现了原码的第二个问题：运算复杂。

\subparagraph{反码的表示方式}
为了解决原码运算复杂的问题，以及计算机无减法运算的问题，我们舍弃了符号位这一表示方法，
进而引入一个新的概念——反码，什么是反码呢？
将原码的每一位取反(01互换)，我们用得到的这个数表示其相对应的负数。
例如-5的反码就是5(0000 0101)每一位取反即1111 1010。
此时，我们再研究$5+(-5)$这个问题，利用反码我们的计算机又要怎么操作呢？
5(0000 0101)直接与-5(1111 1010)相加得到1111 1111，这个值刚好为-0。
这样一来，我们似乎既规避掉了计算机没有减法运算的问题，又完美解决了原码计算复杂的问题。
但这种表示方法还存在两个问题需要解决：正负零问题；溢出问题。

正负零问题是先前便存在的。但新出现的溢出问题是什么呢？
我们来看$2-1$是怎样计算的呢？由于计算机没有减法运算，我们将这一式子化为$2+(-1)$，
也就是2(0000 0010)+(-1)(1111 1110)，相加可得1 0000 0000，我们发现数据出现第九位了，
我们称这种情况为溢出，在反码运算中，我们必须让溢出位加进结果的最低位，也即0000 0001，这样才能保证反码运算的正确性。
而这一操作也给计算机带来了一定的麻烦。

\subparagraph{补码的表示方法}
那是否存在有一种既能解决正负零又能让计算机运算方便的表示方式呢？答案当然是有的——补码。
类比时钟的运转，我们将超出模最大值的数继续用负数表示，
也即用128$\sim $255分别表示-128$\sim $-1。
举一个例子：-6的补码表示为模256-6=250(11111010)。
这样一来我们就将原本前后不连续的数，连接了起来，也解决了正负零的问题。

然后我们研究一下要如何进行补码计算呢？
$$6-4=6+(-4)=6+(256-4)=6+252
=0000 0110+1111 1100=0000 0010=2$$
我们惊奇的发现我们只需要将负数的补码表示与正数的原码表示相加，溢出位舍去我们就可以得到正确的答案。

那么计算机又是如何处理补码这个概念的呢？
通过大量计算与经验总结，我们可以得到将负数取绝对值得到的正数的原码取反然后加1，可以得到负数的补码。
接下来我们以计算机的思路处理一遍负数：
当计算机获取到-x时，检测到负号，计算机进行如下操作：
取其后的正值x；
将正值的原码逐位取反然后加1，这样就得到了原本-x的补码了。

至此，我们通过补码表示法也完美的解决了负数在计算机中的所有问题。


%第二部分
\section{逻辑代数基础}

\subsection{逻辑运算}

\subsection{逻辑函数及其化简}

\subsubsection{逻辑函数}

\subsubsection{逻辑函数的化简}

\begin{enumerate}
    \item 代数法
    

    \item 卡诺图
        \subitem 不含无关项的卡诺图

        \subitem 含有无关项的卡诺图
        
\end{enumerate}


%第三部分
\section{数字电路分析与设计思路}

我们根据数字系统的逻辑功能和电路结构，将数字电路分为：组合逻辑电路和时序逻辑电路。
%（待补充：组合逻辑电路与时序逻辑电路的区别）

\subsection{组合逻辑电路}
%（待补充：组合逻辑电路的定义）

\subsubsection{组合逻辑电路的分析}

\begin{enumerate}
    \item 
\end{enumerate}

\subsubsection{组合逻辑电路的设计思路}

\subsection{时序逻辑电路}


\section{组合逻辑电路的电路模块与应用}

\subsection{编码器}
将输入的有效信号，转换为电平信号的形式，再产生二进制代码与其对应的逻辑电路模块。
    \begin{enumerate}
        \item 二进制编码器
        
        用n位二进制代码对$2^{n}$个信号进行编码的电路
            \subitem {\heiti 普通编码器}

                我们常用的普通编码器有8线-3线编码器，也即有8个输入端，3个输出端的电路。
                我们规定在任意时刻，其输入仅有一个取值为1，且其输入与输出一一对应。其真值表如下：

                \begin{figure}[H]
    
                    \centering
                    \includegraphics[scale=0.4]{Pic/8线3线编码器真值表.jpg}
                    \caption{8线-3线编码器真值表}
                    \label{fig:label}
                \end{figure}

                根据真值表我们可以写出$B_{2}$、$B_{2}$、$B_{2}$的表达式，举例$B_{2}$的一个最小项如下：
                $$\overline{A_{0}}\cdot\overline{A_{1}}\cdot\overline{A_{2}}\cdot\overline{A_{3}}\cdot A_{4}\cdot \overline{A_{5}}\cdot \overline{A_{6}}\cdot \overline{A_{7}}$$
                该最小项与其他无关项合并最后可得
                $$A_{4}$$
                同理，我们可以得到
                \begin{equation}
                    B_{2} = A_{4}+A_{5}+A_{6}+A_{7}
                \end{equation}
                同理，我们又可得另外两个输出，
                \begin{equation}
                    B_{1} = A_{2}+A_{3}+A_{6}+A_{7}
                \end{equation}
                \begin{equation}
                    B_{0} = A_{1}+A_{3}+A_{5}+A_{7}
                \end{equation}
                其逻辑电路如下：

                \begin{figure}[H]
    
                    \centering
                    \includegraphics[scale=0.5]{Pic/8线3线编码器逻辑电路.png}
                    \caption{8线-3线编码器逻辑电路}
                    \label{fig:label}
                \end{figure}
            
            
            \subitem {\heiti 优先编码器}

                上述的普通编码器虽然达到了编码控制电路的目的，但是任意时刻仅允许一个输入信号有效。
                然而对于我们常见的应用场景，我们往往需要多个有效输入信号，因此我们通过给所有信号赋予优先顺序，
                当多个信号有效时，仅对优先级最高的信号进行编码。于是，我们将按优先级从高至低排列，有以下编码方式：
                
                \begin{figure}[H]
                    \centering
                    \includegraphics[scale=0.8]{Pic/优先编码器引言.png}
                    \caption{引入优先级编码方式}
                    \label{fig:label}
                \end{figure}
                从真值表我们可以看出有三组不同的输入有相同的输出，不符合编码器的输入输出唯一性，实际使用会产生异常。
                因此，我们再引入一个输入端口，称为使能输入端，该端口输入的信号称为选通信号，记为$S$(select)。

                我们将允许多个有效信号输入的编码器称为优先编码器。我们常用的74LS148就是一种8线-3线优先编码器，其真值表如下：

                \begin{figure}[H]
                    \centering
                    \includegraphics[scale=0.3]{Pic/优先编码器真值表.jpg}
                    \caption{74LS148真值表}
                    \label{fig:label}
                \end{figure}

                其逻辑图如下：
                
                %缺图

                从真值表我们可以得到{\heiti 74LS148}的输入输出端都是{\heiti 低电平有效}，且可以得到以下表达式：
                \begin{equation}
                    \begin{aligned}
                        F_{2} 
                        & =S\cdot I_{7}+S\cdot I_{6}\cdot \overline{I_{7}}+S\cdot I_{5}\cdot \overline{I_{6}}\cdot \overline{I_{7}}+S\cdot I_{4}\cdot \overline{I_{5}}\cdot \overline{I_{6}}\cdot \overline{I_{7}} \\
                        &=S\cdot (I_{7}+I_{6}+I_{5}+I_{4})
                    \end{aligned}
                \end{equation}
                可以看出，{\heiti 当选通信号$\overline{S}=0$时，其他输入可以正常工作；
                当$\overline{S}=1$时，其他输出被封锁，输出均为高电平。}

                从逻辑图可以得到$F_{S}$和$\overline{F_{EX}}$的表达式：
                \begin{equation}
                    \begin{aligned}
                        F_{S}&=I_{0}+I_{1}+I_{2}+...+I_{7}+\overline{S}\\
                        &=\overline{\overline{I_{0}}\cdot \overline{I_{1}}\cdot \overline{I_{2}}\cdot ...\cdot\overline{I_{7}}\cdot S}\\
                        &=\overline{\overline{I_{0}+I_{1}+I_{2}+...+I_{7}}\cdot S}
                    \end{aligned}
                \end{equation}

                \begin{equation}
                    \begin{aligned}
                        \overline{F_{EX}}&=\overline{F_{S}\cdot S}\\
                        &=\overline{S}+S\cdot \overline{I_{0}+I_{1}+I_{2}+...+I_{7}}\\
                        &=\overline{S}+\overline{I_{0}+I_{1}+I_{2}+...+I_{7}}\\
                        &=\overline{S\cdot (I_{0}+I_{1}+I_{2}+...+I_{7})}
                    \end{aligned}
                \end{equation}

                从(5)的表达式中，我们可以得知
                只有当$\overline{S}=0$，也即其他输入正常工作，
                且不存在有效信号时，$F_{S}$才输出为低电平。
                因此，{\heiti $F_{S}$输出低电平表示该电路正常工作，且无有效信号输入。}
                我们称$F_{S}$为{\heiti 使能输出端}。

                从(6)的表达式中，我们可以得知
                只有当$\overline{S}=0$，也即其他输入正常工作，
                且存在有效信号时，$F_{EX}$才输出为低电平。
                因此，{\heiti $F_{EX}$输出低电平表示该电路正常工作，且存在有效信号输入。}
                我们称$F_{EX}$为{\heiti 扩展输出端}。


        \item 二-十进制编码器
        
    \end{enumerate}

\subsection{译码器}

\subsection{数据选择器}

\subsection{数值比较器}

\subsection{加法器}

\end{document}